from django.apps import AppConfig


class TimetableSystemConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "timetable_system"
    verbose_name = "Timetable System"
